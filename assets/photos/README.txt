✅ Your 11 photos are already added and wired up:

  hero.jpg      -> your matching-white-outfits photo (opening "Happy Birthday" screen)
  ending.jpg     -> the tree selfie kiss photo (very last, closing screen)
  memory1.jpg … memory9.jpg  -> the rest, shown in the Memory Wall (chapter five)

Want to swap any of them or change captions? Open js/script.js and find the
"memories" array near the top — each line is one photo + its caption, in the
same order they appear on the wall. Just edit the caption text, or replace a
photo file (keep the same filename) to change the picture without touching code.

Want to add even more memory photos? Add your own images as memory10.jpg,
memory11.jpg, etc. into this folder, then add matching lines to that array.
